# ============================================================
# 部署配置（复制为 .deploy.env 并填入真实值）
#   cp .deploy.env.example .deploy.env
# 注意：.deploy.env 含服务器信息，已在 .gitignore 中忽略，不要提交
# ============================================================
SERVER_IP=117.72.207.28
SERVER_USER=root
SERVER_PORT=22
SERVER_DIR=/www/wwwroot/ai-inquiry
