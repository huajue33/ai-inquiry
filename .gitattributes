* text=auto

# Shell 脚本必须 LF，避免 Windows 上 git checkout 转 CRLF 导致容器内执行失败
*.sh text eol=lf
entrypoint.sh text eol=lf
Dockerfile text eol=lf
*.dockerfile text eol=lf

# 二进制
*.png binary
*.jpg binary
*.svg text
